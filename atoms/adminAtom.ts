import { atom } from "jotai";
export const createCategory = atom<boolean>(false);
export const createPostCategory = atom<boolean>(false);
export const createProduct = atom<boolean>(false);
export const createImage = atom<boolean>(false);
export const createPost = atom<boolean>(false);